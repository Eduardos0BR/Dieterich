# Copie este arquivo para .env e preencha os valores reais
# cp .env.example .env

# ── Segurança ──────────────────────────────────────────────
SECRET_KEY=troque-por-uma-chave-secreta-longa-e-aleatoria

# ── Banco de dados ─────────────────────────────────────────
# SQLite (padrão — não precisa alterar para desenvolvimento)
# DATABASE_URL=sqlite:///instance/dieterich.db

# PostgreSQL (produção)
# DATABASE_URL=postgresql://usuario:senha@localhost:5432/dieterich

# ── WhatsApp ──────────────────────────────────────────────
WHATSAPP_NUMBER=5547999999999

# ── E-mail ────────────────────────────────────────────────
MAIL_FROM=contato@dieterich.com.br
ADMIN_EMAIL=admin@dieterich.com.br

# ── Loja ──────────────────────────────────────────────────
# (opcional — sobrescreve os valores padrão em config.py)
# STORE_ADDRESS=Rua Real, 456 — Centro, Rio do Sul – SC
# STORE_PHONE=(47) 9 8888-8888
# GOOGLE_MAPS_URL=https://maps.google.com/?q=...
